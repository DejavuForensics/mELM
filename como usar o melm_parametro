🔹 1. Execução simples (treino/teste separados, sem busca/KFold)
Se você tem arquivos treino e teste já separados:
python melmParameters.py -tr treino.csv -ts teste.csv -ty 1 -nh 100 -af sigmoid
    • -ty 1 → classificação (use 0 para regressão)
    • -nh → número de neurônios ocultos (aqui 100)
    • -af → função de ativação (sigmoid, linear, radbas, etc.)

🔹 2. Execução com AllData_File + KFold + busca
👉 Este é o caminho que gera o elm_report.html com Melhor/Pior desempenho.

python melmParameters.py -tall dados.csv -ty 1 -nh 50,100,200 -af sigmoid,linear,radbas -kfold 10

    • -tall dados.csv → arquivo único (com todas as amostras)
    • -ty 1 → classificação (0 para regressão)
    • -nh 50,100,200 → vai testar 3 valores de neurônios ocultos
    • -af sigmoid,linear,radbas → vai testar 3 ativações diferentes
    • -kfold 10 → validação cruzada 10-fold (com shuffle)
Saída esperada:
    • Prints no terminal com melhores/piores resultados
    • Arquivo elm_report.html + imagens em elm_report_images/

🔹 3. Exemplo mínimo só com busca em duas ativações
python melmParameters.py -tall dataset.csv -ty 1 -nh 100 -af sigmoid,radbas -kfold 5
Esse comando:
    • Testa sigmoid e radbas com 100 neurônios,
    • Faz 5-fold CV,
    • Escolhe melhor/pior por acurácia de teste,
    • Salva relatório em elm_report.html.

